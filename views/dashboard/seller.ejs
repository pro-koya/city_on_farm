<%-
  include('layout', {
    title,
    active: 'seller',
    body: `
      ${include('./_cards/listings', { listings })}
      ${include('./_cards/trades',   { trades })}
      ${include('./_cards/revenue',  { revenue })}
    `
  })
%>