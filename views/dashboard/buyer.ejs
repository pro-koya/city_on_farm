<%-
  include('layout', {
    title,
    active: 'buyer',
    body: `
      ${include('./_cards/buyer-actions',  { })}
      ${include('./_cards/orders',  { orders })}
      ${include('./_cards/favorites',  { favorites })}
      ${include('./_cards/recent',  { recent })}
      ${include('./_cards/notices', { notices })}
    `
  })
%>