<%-
  include('layout', {
    title,
    active: 'buyer',
    body: `
      ${include('./_cards/orders',  { orders })}
      ${include('./_cards/recent',  { recent })}
      ${include('./_cards/notices', { notices })}
    `
  })
%>