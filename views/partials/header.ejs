<!DOCTYPE html>
<html lang="ja">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- SEO: タイトル -->
  <title><%= title %> | セッツマルシェ - 新鮮な農作物を農家から直送</title>

  <!-- SEO: メタディスクリプション -->
  <%
    const defaultDescription = "セッツマルシェは地域の農家が育てた新鮮な農作物を直接お届けするオンラインマーケット。旬の野菜や果物、こだわりの農産物を生産者から直接購入できます。地産地消で安心・安全な食卓をサポートします。";
    const pageDescription = typeof description !== 'undefined' ? description : defaultDescription;
  %>
  <meta name="description" content="<%= pageDescription %>" />

  <!-- SEO: キーワード -->
  <meta name="keywords" content="農作物,直売,産直,地産地消,新鮮野菜,果物,農家直送,オーガニック,有機野菜,旬の野菜,セッツマルシェ,今日の食卓,マルシェ,農産物" />

  <!-- SEO: 著者・サイト名 -->
  <meta name="author" content="セッツマルシェ" />
  <meta name="application-name" content="セッツマルシェ" />

  <!-- SEO: robots -->
  <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />

  <!-- SEO: canonical URL -->
  <% if (typeof canonicalUrl !== 'undefined' && canonicalUrl) { %>
    <link rel="canonical" href="<%= canonicalUrl %>" />
  <% } %>

  <!-- Open Graph / Facebook -->
  <%
    const siteName = "セッツマルシェ";
    const ogTitle = typeof ogTitleCustom !== 'undefined' ? ogTitleCustom : title + ' | ' + siteName;
    const ogImage = typeof ogImageUrl !== 'undefined' ? ogImageUrl : '/images/setsumaru-logo.png';
    const ogUrl = typeof canonicalUrl !== 'undefined' ? canonicalUrl : '';
  %>
  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="<%= siteName %>" />
  <meta property="og:title" content="<%= ogTitle %>" />
  <meta property="og:description" content="<%= pageDescription %>" />
  <meta property="og:image" content="<%= ogImage %>" />
  <% if (ogUrl) { %>
    <meta property="og:url" content="<%= ogUrl %>" />
  <% } %>
  <meta property="og:locale" content="ja_JP" />

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="<%= ogTitle %>" />
  <meta name="twitter:description" content="<%= pageDescription %>" />
  <meta name="twitter:image" content="<%= ogImage %>" />

  <!-- Favicon & Icons -->
  <link rel="icon" type="image/png" href="/images/setsumaru-logo.png" />
  <link rel="apple-touch-icon" href="/images/setsumaru-logo.png" />
  <link rel="shortcut icon" href="/images/setsumaru-logo.png" />

  <!-- Theme Color (ブラウザのテーマカラー) -->
  <meta name="theme-color" content="#4C6B5C" />
  <meta name="msapplication-TileColor" content="#4C6B5C" />

  <!-- Stylesheets -->
  <link rel="stylesheet" href="/styles/styles.css" />
  <% if (extraCSS) { %>
    <link rel="stylesheet" href="<%= extraCSS %>" />
  <% } %>
  <% if (extraCSS2) { %>
    <link rel="stylesheet" href="<%= extraCSS2 %>" />
  <% } %>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
  <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/themes/material_green.css"> -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/plugins/weekSelect/weekSelect.css">
</head>

<body>
  <header class="navbar">
    <div class="navbar-container">
      <!-- 左：ロゴ -->
      <a href="/" class="navbar-logo"><img src="/images/setsumaru-logo.png" alt="セッツマルシェ"></a>

      <!-- 右：PCもモバイルもトグル＋（PCのみ）ログインピル -->
      <div class="navbar-right">
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
          <!-- PC専用のログインピル（モバイルでは非表示） -->
          <span class="nav-loginstatus__pill nav-loginstatus--desktop" aria-live="polite">
            <strong>
              <%= currentUser.name %>
            </strong> さんとしてログイン中
          </span>

          <!-- ✅ お知らせベル -->
          <% const unread = (typeof unreadNoticeCount !== 'undefined' ? unreadNoticeCount : 0); %>
            <a href="/my/notifications" class="navbar-notice" aria-label="お知らせ">
              <!-- ベルアイコン（シンプルなSVG） -->
              <svg class="notice-icon" width="22" height="22" viewBox="0 0 24 24" aria-hidden="true">
                <path fill="currentColor"
                  d="M12 2a4 4 0 0 0-4 4v1.1C6.3 8.3 5 10.1 5 12v4l-1 1v1h16v-1l-1-1v-4c0-1.9-1.3-3.7-3-4.9V6a4 4 0 0 0-4-4zm0 2a2 2 0 0 1 2 2v1h-4V6a2 2 0 0 1 2-2zm0 16a2.5 2.5 0 0 0 2.45-2h-4.9A2.5 2.5 0 0 0 12 20z" />
              </svg>
              <% if (unread > 0) { %>
                <span class="notice-badge" aria-live="polite">
                  <%= unread > 99 ? '99+' : unread %>
                </span>
              <% } else { %>
                <!-- 未読0のときは小さなドットだけ（任意） -->
                <span class="notice-dot" aria-hidden="true"></span>
              <% } %>
            </a>
          <% } %>

            <!-- ✅ カートボタン -->
            <a href="/cart" class="navbar-cart" aria-label="カートへ">
              <!-- ショッピングカートの簡易SVGアイコン -->
              <svg class="cart-icon" width="22" height="22" viewBox="0 0 24 24" aria-hidden="true">
                <path fill="currentColor"
                  d="M7 18a2 2 0 1 0 0 4a2 2 0 0 0 0-4m10 0a2 2 0 1 0 0 4a2 2 0 0 0 0-4M7.2 14h9.9c.8 0 1.5-.5 1.7-1.3l1.7-6.3c.1-.5-.2-1-.8-1H6.2L5.7 3H3v2h1.2l2.3 9c.2.7.9 1.2 1.7 1.2Z" />
              </svg>
              <span class="cart-badge" aria-live="polite">
                <%= (typeof cartCount!=='undefined' ? cartCount : 0) %>
              </span>
            </a>

            <button class="navbar-toggle" id="navbar-toggle" aria-label="メニューを開閉" aria-controls="navbar-menu"
              aria-expanded="false">
              <span id="toggle-icon">☰</span>
            </button>
      </div>
    </div>

    <!-- 共通：ハンバーガーメニュー（PC/モバイル同一UI） -->
    <ul class="navbar-menu" id="navbar-menu" role="menu">
      <li role="none"><a role="menuitem" href="/">ホーム</a></li>
      <li role="none"><a role="menuitem" href="/products">農作物一覧</a></li>
      <li role="none"><a role="menuitem" href="/blog">ブログ</a></li>
      <li role="none"><a role="menuitem" href="/contact">お問い合わせ</a></li>
      <li role="none"><a role="menuitem" href="/cart">カート（<%= (typeof cartCount!=='undefined' ? cartCount : 0) %>）</a>
      </li>
      <% if (typeof currentUser !== 'undefined' && currentUser) { %>
        <% const unread = (typeof unreadNoticeCount !== 'undefined' ? unreadNoticeCount : 0); %>
        <li role="none">
          <a role="menuitem" href="/my/notifications">
            お知らせ
            <% if (unread > 0) { %>
              （<%= unread > 99 ? '99+' : unread %>）
            <% } %>
          </a>
        </li>

        <!-- 出品者用：マイショップへのリンク -->
        <% if (currentUser.roles?.includes('seller') && currentUser.partner_id) { %>
          <li role="none"><a role="menuitem" href="/admin/partners/<%= currentUser.partner_id %>">🏪 マイショップ</a></li>
        <% } %>

        <li role="none"><a role="menuitem"
            href="/dashboard/<%= (currentUser.roles?.includes('seller') ? 'seller' : 'buyer') %>">ダッシュボード</a></li>
        <li><a href="/admin/users/<%= currentUser.id %>">アカウント情報</a></li>
        <li class="nav-logout" role="none">
          <form method="POST" action="/logout">
            <% if (typeof csrfToken !=='undefined' && csrfToken) { %>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <button type="submit" class="linklike" role="menuitem">ログアウト</button>
              <% } %>
          </form>
        </li>

        <!-- モバイル専用：メニュー最下部にログインピル（PCでは非表示） -->
        <li class="nav-loginstatus" aria-live="polite">
          <span class="nav-loginstatus__pill">
            <strong>
              <%= currentUser.name %>
            </strong> さんとしてログイン中
          </span>
        </li>
        <% } else { %>
          <li role="none"><a role="menuitem" href="/login">ログイン</a></li>
          <li role="none"><a role="menuitem" href="/signup">サインアップ</a></li>
          <% } %>
    </ul>
  </header>

  <main>